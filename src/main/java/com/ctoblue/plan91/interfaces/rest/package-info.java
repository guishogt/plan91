/**
 * REST API controllers - HTTP endpoints for HTMX interactions.
 *
 * <p>This package contains REST controllers that expose APIs for:
 * <ul>
 *   <li>Habit management (create, update, delete, complete)</li>
 *   <li>Category management</li>
 *   <li>Analytics and statistics</li>
 *   <li>User profile</li>
 * </ul>
 *
 * <p>These endpoints return JSON for HTMX to consume and dynamically
 * update the UI without full page reloads.
 *
 * <p>DTOs and mappers handle conversion between API representations
 * and domain objects.
 *
 * @since 0.1.0
 */
package com.ctoblue.plan91.interfaces.rest;
